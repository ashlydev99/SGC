# SGC - Sistema de Gestión de Clientes

## Requisitos previos para compilar en Linux:
- Java JDK 17 o superior
- Android SDK (recomendado Android Studio o command line tools)

## Instalación de dependencias en Linux:

### 1. Instalar Java 17:
```bash
sudo apt update
sudo apt install openjdk-17-jdk